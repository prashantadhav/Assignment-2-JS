// Define a function called "multiplyByTwo" that takes one parameter: "num"
function multiplyByTwo(num) {
    // Multiply the input number by two and return the result
    return num * 2;
  }
  
  // Test the function by calling it with a sample input
  console.log(multiplyByTwo(5)); // Output: 10
  